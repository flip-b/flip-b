import {Base} from './base';
import {Item} from './item';

/**
 * Menu
 */
export class Menu extends Base {
  // Definitions

  /**
   * Error
   * @attribute {Mixed}
   */
  error: any | undefined;

  /**
   * Value
   * @attribute {Mixed}
   */
  value: any | undefined;

  /**
   * Setup
   */
  setup() {}
}
